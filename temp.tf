
  


    
 

    

  
    #   "${var.vmresource_group.name}" = "vm-rg"
    # "${var.resource_group.location}" = "SouthEast Asia"
    # "${var.vmprofile.vm_name}"  = ""
    # "${var.vmprofile.vm_location}" = ""
    # "${var.vmprofile.vm_size}" = ""
    # "${var.vmprofile.vnet_subnet_id}" = "111"
    # "${var.vmprofile.os_profile}" = ""
    # "${var.os_image.publisher}" = "Canonical"
    # "${var.os_image.offer}"     = "UbuntuServer"
    # "${var.os_image.sku}"       = "16.04-LTS"
    # "${var.os_image.version}"   = "latest"
    # "${var.os_profile.computer_name}"  = "hostname"
    # "${var.os_profile.admin_username}" = "testadmin"
    # "${var.os_profile.admin_password}" = "Password1234" 